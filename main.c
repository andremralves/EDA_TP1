#include <stdio.h>

int main() {
    int menuNum;

    do {
        scanf("%d", &menuNum);
        switch (menuNum) {
            case 1:

                break;
            case 2:
                break;
            case 3:
                break;
        }
    } while (menuNum != 0);

    return 0;
}
